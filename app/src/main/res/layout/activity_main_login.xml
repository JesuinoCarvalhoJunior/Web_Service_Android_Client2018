<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/drawer_layout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:weightSum="1"
    tools:context="com.wsandroid.jcarvalhojr.webserviceandroidclient.MainActivity">
    <!--pull to Refresh com SwipeRefreshLayout  -->
    <android.support.v4.widget.SwipeRefreshLayout
        android:id="@+id/swipeToRefresh"
        android:layout_width="match_parent"
        android:layout_height="wrap_content">


        <android.support.v7.widget.RecyclerView
            android:id="@+id/recyclerViewLista"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:cacheColorHint="@android:color/transparent"
            android:clipToPadding="false"
            android:divider="@null"
            android:dividerHeight="0dp"
            android:listSelector="@android:color/transparent"
            android:orientation="horizontal"
            android:overScrollMode="never"
            android:scrollbarStyle="outsideOverlay"
            android:scrollbars="vertical"
            android:textColor="@color/text_color_inverse" />

    </android.support.v4.widget.SwipeRefreshLayout>
    <!--
      <Button
         android:id="@+id/btnAbreTelaCadastro"
         android:layout_width="45dp"
         android:layout_height="45dp"
         android:layout_alignEnd="@+id/lstvUsuarios"
         android:layout_alignParentBottom="true"
         android:layout_alignParentLeft="true"
         android:layout_alignParentStart="true"
         android:layout_alignRight="@+id/lstvUsuarios"
         android:layout_gravity="bottom"
         android:text="Administrar"
         />
             -->

    <!--
   <ListView
       android:id="@+id/lstvUsuarios"
       android:layout_width="wrap_content"
       android:layout_height="wrap_content"
       android:layout_above="@+id/btnAbreTelaCadastro"
       android:layout_alignParentEnd="true"
       android:layout_alignParentRight="true"
       android:layout_alignParentTop="true"
       android:background="#c4e6e7"
       android:divider="#aa00ff1e"
       android:dividerHeight="1sp"
       android:listSelector="@drawable/list_color_selector"
       android:scrollbars="vertical"
       android:typeface="sans"
       />
   -->
    <!-- android:textColor="@drawable/list_item_text_selector" -->

    <!-- Framelayout to display Fragments

    <FrameLayout
        android:id="@+id/frame_container"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />
    -->


</FrameLayout>